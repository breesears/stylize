<!DOCTYPE hmtl>
  <html>
    <head>
      <title><%%= content_for?(:title) ? yield(:title)  :  "Rails" %></title>
      <%%= stylesheet_link_tag "<%= file_name %>/<%= file_name %>" %>
      <%%= javascript_include_tag :defaults %>
      <%%= csrf_meta_tag %>
      <%%= yield(:head) %>

    </head>
    <body>
      <div id="container">
	   	 
	   
	     <div id="content">
			
                <!-- <%% flash.each do |name, msg| %>
                <%%= content_tag :div, msg, :id => "flash_#{name}" %>
                <%% end %>
                <%%= content_tag :h1, yield(:title) if show_title? %> -->
                <%%= yield %>
         </div>
      </div>
    </body>
</html>
